[package]
name = "git-switch"
version = "0.1.0"
edition = "2024"

[dependencies]
dialoguer = "0.11"
indicatif = "0.17"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
