[package]
name = "git-switch"
version = "1.0.0"
edition = "2024"

[dependencies]
dialoguer = "0.12"
indicatif = "0.17"

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
